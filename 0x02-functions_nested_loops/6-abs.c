#include "main.h"

/**
 * _abs - Entry point
 * @t: the interger to be generated
 * Decription: a funtion that computes absolute value of an integer
 *
 * Return: Absolute valu
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
